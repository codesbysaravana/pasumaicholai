# Production Environment Variables for Client
# Since Nginx is reverse-proxying /api/ to the backend on the same server,
# we can use a relative URL. This avoids CORS issues and automatically supports both IP addresses and domains!
VITE_API_BASE_URL=https://pasumaicholai-server-nnuf.onrender.com
