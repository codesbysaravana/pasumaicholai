import { ModuleCard } from "../../../components/ui/ModuleCard";

export function DeliveryTask() {
  return <ModuleCard title="Accept Tasks" description="Accept or reject delivery tasks based on route and load." ctaLabel="Manage Tasks" />;
}
